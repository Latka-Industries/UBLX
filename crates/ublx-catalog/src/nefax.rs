//! Nefaxer type aliases used by the snapshot tables (no runner: `nefax_dir` stays in `ublx`).

pub type NefaxResult = nefaxer::Nefax;
pub type NefaxDiff = nefaxer::Diff;
pub type NefaxPathMeta = nefaxer::PathMeta;
